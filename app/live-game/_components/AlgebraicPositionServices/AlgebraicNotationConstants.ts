export enum Files {
    a,
    b,
    c,
    d,
    e,
    f,
    g,
    h,
}
export enum PieceValues {
    pawn = 1,
    knight = 3,
    // eslint-disable-next-line @typescript-eslint/no-duplicate-enum-values
    bishop = 3,
    rook = 5,
    queen = 9,
}

